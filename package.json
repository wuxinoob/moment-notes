{
  "name": "moment-notes",
  "version": "1.6.0",
  "private": true,
  "description": "拾光便签：一款基于 Vue 3 + TypeScript + Vite 构建的高颜值、高生产力的 uTools 便签插件。",
  "type": "module",
  "scripts": {
    "dev": "node scripts/clean-utools.js && vite",
    "dev:utools": "node scripts/copy-utools.js --dev && vite",
    "build": "node scripts/clean-utools.js && vue-tsc --noEmit && vite build",
    "build:web": "node scripts/clean-utools.js && vue-tsc --noEmit && vite build",
    "build:utools": "vue-tsc --noEmit && vite build && node scripts/copy-utools.js",
    "preview": "vite preview",
    "sync:kenv": "npm run build && node -e \"const fs=require('fs'),path=require('path'),dest=path.join(process.env.USERPROFILE||process.env.HOME,'.kenv','assets','moment-notes','dist');fs.mkdirSync(dest,{recursive:true});fs.cpSync('dist',dest,{recursive:true});console.log('Synced dist to kenv assets');\"",
    "test": "vitest run",
    "lint": "eslint src --fix",
    "lint:nocache": "eslint src --fix --no-cache",
    "deploy": "node scripts/deploy.js",
    "prepare": "husky"
  },
  "dependencies": {
    "@lucide/vue": "^1.24.0",
    "@types/dompurify": "^3.0.5",
    "dompurify": "^3.4.12",
    "marked": "^18.0.7",
    "pinia": "^3.0.4",
    "vue": "^3.5.35"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.3.0",
    "@commitlint/config-conventional": "^19.2.2",
    "@types/node": "^25.9.1",
    "@typescript-eslint/eslint-plugin": "^7.8.0",
    "@typescript-eslint/parser": "^7.8.0",
    "@vitejs/plugin-vue": "^6.0.7",
    "esbuild": "^0.28.2",
    "eslint": "^8.57.0",
    "eslint-plugin-vue": "^9.25.0",
    "husky": "^9.0.11",
    "lint-staged": "^15.2.2",
    "sass": "^1.100.0",
    "typescript": "^6.0.3",
    "utools-api-types": "^7.5.1",
    "vite": "^8.0.16",
    "vitest": "^4.1.10",
    "vue-tsc": "^3.3.3"
  },
  "lint-staged": {
    "src/**/*.{ts,tsx,vue}": [
      "eslint --fix"
    ]
  }
}
